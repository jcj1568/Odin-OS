[project]
name = "odin"
version = "0.1.0"
description = "Odin — Hymdal Labs' personal operating agent"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.1.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    "supabase>=2.7.0",
]

[project.optional-dependencies]
dev = ["ruff>=0.6.0"]

[tool.uv]
package = false
